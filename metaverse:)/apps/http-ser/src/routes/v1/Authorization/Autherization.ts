import { Router, type Request, type Response } from "express";

export const Autherization = Router();

Autherization.post("/signup", (req: Request,res: Response) => {
    
})

Autherization.post("/signin", (req: Request, res: Response) => {
    
})